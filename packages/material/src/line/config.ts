import { CARD_KEYS } from '../constants'

const config = {
  siderConfig: {
    key: CARD_KEYS.LINE,
    name: '折线图',
    groupName: '常用图表',
    icon: 'icon-tubiao-zhexiantu',
    description: '折线图',
  },

  styleConfig: {},

  dataConfig: {},

  eventConfig: {},
}

export default config
